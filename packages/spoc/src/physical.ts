export const MEAN_WHEEL_SHAFT_LENGTH = 200 // mm

export const LEFT_WHEEL_SPEED_TO_PWM = (
  speed: number,
  inputVoltage: number
) => {
  return 255
}

export const RIGHT_WHEEL_SPEED_TO_PWM = (
  speed: number,
  inputVoltage: number
) => {
  return 255
}
